name: Build

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-windows:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: pip install pywebview pyinstaller
      - name: Build 가계부.exe
        run: pyinstaller budget.spec
      - name: Build updater.exe
        run: pyinstaller updater.spec
      - name: Move updater to dist
        run: copy dist\updater.exe dist\updater.exe
      - name: Upload Windows
        uses: actions/upload-artifact@v4
        with:
          name: 가계부-Windows
          path: |
            dist/가계부.exe
            dist/updater.exe

  build-mac:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      - name: Install dependencies
        run: pip install pywebview pyinstaller
      - name: Build app
        run: pyinstaller budget.spec
      - name: Zip app
        run: cd dist && zip -r 가계부-Mac.zip 가계부.app
      - name: Upload Mac
        uses: actions/upload-artifact@v4
        with:
          name: 가계부-Mac
          path: dist/가계부-Mac.zip
